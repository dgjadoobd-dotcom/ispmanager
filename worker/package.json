{
  "name": "ispmanager-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init": "wrangler init"
  },
  "devDependencies": {
    "hono": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
